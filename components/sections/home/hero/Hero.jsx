import { Button } from '@/components/shared/buttons/Button';

const Hero = () => {
  return (
    <div className="h-screen">
      <Button variant="primary" size="xl" className="mt-10 ml-10">
        Get Started
      </Button>
    </div>
  );
};

export default Hero;
